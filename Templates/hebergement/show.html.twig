{% extends 'structure/base.html.twig' %}

{% block title %}Hebergement{% endblock %}
{% block bandeau %}
        <div class="col-md-12" style="padding-left: 0px;">
            <img src="{{ asset('images/imgConcept.png') }}" alt="" width="100%" height="100%" style="margin: 0; padding: 0; position: relative;"/>
        </div>
{% endblock %}
{% block body %}

{% if app.request.locale == "fr" %}
<main role="main" class="container"><br><br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <h3 class="text-center">
                    {{ hebergement.grandTitre | raw }}
                </h3><br>
                <p class="text-center text-muted">
                   {{ hebergement.descriptionSsTitre | raw }}
                </p>
                <center><img alt="" src="{{ asset('upload/'~hebergement.image1~'') }}" class="img-thumbnail" width="100%" height="20%" /></center>
                <br><h3 class="text-center">
                    {{ hebergement.typeDeChambre | raw }}
                </h3>
                <p class="text-center text-muted">
                    {{ hebergement.ssText | raw }}
                </p>
                <center>{{ hebergement.galerie | raw }}</center>
            </div>
        </div>
    </div>
{% endif %}
{% if app.request.locale == "en" %}
<main role="main" class="container"><br><br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <h3 class="text-center">
                    {{ hebergement.grandTitreEn | raw }}
                </h3><br>
                <p class="text-center text-muted">
                   {{ hebergement.descriptionSsTitreEn | raw }}
                </p>
                <center><img alt="" src="{{ asset('upload/'~hebergement.image1~'') }}" class="img-thumbnail" width="100%" height="20%" /></center>
                <br><h3 class="text-center">
                    {{ hebergement.typeDeChambreEn | raw }}
                </h3>
                <p class="text-center text-muted">
                    {{ hebergement.ssTextEn | raw }}
                </p>
                <center>{{ hebergement.galerie | raw }}</center>
            </div>
        </div>
    </div>
{% endif %}
{% if app.request.locale == "es" %}
<main role="main" class="container"><br><br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <h3 class="text-center">
                    {{ hebergement.grandTitreEs | raw }}
                </h3><br>
                <p class="text-center text-muted">
                   {{ hebergement.descriptionSsTitreEs | raw }}
                </p>
                <center><img alt="" src="{{ asset('upload/'~hebergement.image1~'') }}" class="img-thumbnail" width="100%" height="20%" /></center>
                <br><h3 class="text-center">
                    {{ hebergement.typeDeChambreEs | raw }}
                </h3>
                <p class="text-center text-muted">
                    {{ hebergement.ssTextEs | raw }}
                </p>
                <center>{{ hebergement.galerie | raw }}</center>
            </div>
        </div>
    </div>
{% endif %}
{% if app.request.locale == "it" %}
<main role="main" class="container"><br><br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <h3 class="text-center">
                    {{ hebergement.grandTitreIt | raw }}
                </h3><br>
                <p class="text-center text-muted">
                   {{ hebergement.descriptionSsTitreIt | raw }}
                </p>
                <center><img alt="" src="{{ asset('upload/'~hebergement.image1~'') }}" class="img-thumbnail" width="100%" height="20%" /></center>
                <br><h3 class="text-center">
                    {{ hebergement.typeDeChambreIt | raw }}
                </h3>
                <p class="text-center text-muted">
                    {{ hebergement.ssTextIt | raw }}
                </p>
                <center>{{ hebergement.galerie | raw }}</center>
            </div>
        </div>
    </div>
{% endif %}
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('hebergement_index') }}">Retour à la liste</a>
    <a href="{{ path('hebergement_edit', {'id': hebergement.id}) }}">Editer </a>
    &nbsp;<a class="btn btn-danger btn-sm">{{ include('hebergement/_delete_form.html.twig') }}
    </a>
    {% endif %}
</main>
{% endblock %}
