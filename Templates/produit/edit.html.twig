{% extends 'structure/base.html.twig' %}

{% block title %}Editer un Cours{% endblock %}

{% block body %}
<div class="container">
    <h1>Editer Cours</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Mettre à jours'}) }}
    <br>
    <a class="btn btn-secondary btn-sm" href="{{ path('produit_index') }}">Retour à la liste des cours</a>
</div>
{% endblock %}
