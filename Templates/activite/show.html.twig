{% extends 'structure/base.html.twig' %}


{% block title %}Activite{% endblock %}
{% block bandeau %}
        <div class="col-md-12" style="padding-left: 0px;">
            <img src="{{ asset('images/imgConcept.png') }}" alt="" width="100%" height="100%" style="margin: 0; padding: 0; position: relative;"/>
        </div>
{% endblock %}
{% block body %}
<main role="main" class="container"><br><br>

    {{ activite.titre | raw }}
    {{ activite.description | raw }}
    <img alt="" src="{{ asset('upload/'~activite.image~'') }}" class="img-thumbnail" width="100%" height="20%" />
            
        </tbody>
    </table>
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('activite_index') }}">Retour à la liste</a>

    <a href="{{ path('activite_edit', {'id': activite.id}) }}">Editer</a>

    &nbsp;<a class="btn btn-danger btn-sm">{{ include('activite/_delete_form.html.twig') }}
        <span class="btn-inner--icon"><i class="fas fa-trash-alt"></i></span>
    </a>
    {% endif %}
{% endblock %}
