<form method="post" action="{{ path('surf_camp_fuert_delete', {'id': surf_camp_fuert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ surf_camp_fuert.id) }}">
    <button class="btn btn-outline-danger btn-sm">Supprimer</button>
</form>
