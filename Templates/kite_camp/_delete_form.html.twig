<form method="post" action="{{ path('kite_camp_delete', {'id': kite_camp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kite_camp.id) }}">
    <button class="btn">Delete</button>
</form>
